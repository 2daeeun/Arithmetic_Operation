#include "sign.h"
int sign()
{
    int y;
    //원래 매개변수에 int y가 있어야 하는데 매개변수 선언을 안 했으니 임시로 int y 만듬.
    int flipsign=0;
    int temp=0;

    if(y < 0)
        temp = 1;
    else
        temp = -1;
    
    while(y != 0)
    {
    }
    return flipsign;
}
